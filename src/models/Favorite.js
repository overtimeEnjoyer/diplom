/**
 * MAK card favorites are stored on User.makFavoriteCardIds (JSONB)
 * for API compatibility with the previous backend contract.
 * This module documents the domain concept for the thesis architecture.
 */
export const FavoriteStorage = 'user.mak_favorite_card_ids';
